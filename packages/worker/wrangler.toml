name = "plugin-marketplace-api"
main = "src/index.ts"
compatibility_date = "2025-01-09"
compatibility_flags = ["nodejs_compat"]

# D1 数据库绑定
[[d1_databases]]
binding = "DB"
database_name = "plugin-marketplace"
database_id = "2e9accf5-8e78-4e9b-8810-bbb0bf9f360e"
migrations_dir = "migrations"

# KV 缓存绑定
[[kv_namespaces]]
binding = "CACHE"
id = "d70868a07c3345058589ca0b1cc2abf2"

# D1 outbox 每 5 分钟补投；每天 UTC 17:00 同时同步 GitHub 仓库元数据。
# 本文件只声明所需触发器；本 PR 不执行部署或修改 Cloudflare 资源。
[triggers]
crons = ["*/5 * * * *", "0 17 * * *"]

# 敏感变量通过 CF Dashboard → Workers → 变量和机密 配置，不写入代码仓库
[vars]
GITHUB_CLIENT_ID = "Ov23licU8Aof4Y08e25v"
REVIEW_SERVICE_URL = "https://review.md-banxia.cn"
