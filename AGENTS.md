# 行为规则

1. 不要假设。不要隐藏困惑。主动暴露权衡取舍。
2. 只写解决当前问题的最小代码，不做任何推测性功能。
3. 只修改必须改的地方，只清理自己产生的问题。
4. 明确定义成功标准，验证通过前持续迭代。

## 反过度工程 / 反防御性编程

- 信任内部代码和框架保证。
- 只在系统边界（用户输入、外部 API、网络）做校验。
- 禁止为「不可能发生」的场景添加错误处理、回退、空值检查或验证。
- 绝不吞掉错误（禁止 `rescue nil` / 宽泛 `catch` / 静默默认值）。
- 禁止为一次性操作创建辅助函数、工具类或抽象。
- 优先快速失败，而不是掩盖问题。

## 本仓库当前开发约束

- 微信小程序 V1 的产品真相以 `docs/10-科研Agent插件导航-PRD-v1.md` 为准。
- 施工顺序与验收标准以根目录 `PLAN.md` 为准。
- 小程序开发分支为 `feature/wechat-miniapp-v1`；不要直接修改 `main`。
- 新增小程序代码放在 `packages/miniapp/`，保持现有 monorepo 结构。
- 现有 `packages/frontend/`、`packages/worker/`、`packages/review-service/` 和 D1 数据结构默认视为稳定底盘；只有当前步骤明确需要时才修改。
- V1 不实现微信登录、用户上传、AI 推荐、排行榜、积分、评论、收藏同步或任何 PRD 明确排除的功能。
- D1 继续作为唯一数据真源；禁止为小程序复制第二份插件数据库或维护独立插件清单。
- 不把 Web 前端代码、React DOM 组件或无关依赖打入小程序构建产物。
- 不部署生产环境、不修改 Cloudflare 生产配置、不提交微信审核，除非任务明确要求。

## 执行方式

- 每次只执行 `PLAN.md` 中一个明确步骤或一个明确子步骤。
- 开始前说明：本次目标、预计修改文件、成功标准、已知权衡。
- 遇到会改变产品范围、数据结构、生产 API、隐私范围或依赖体积的重要不确定性时，停止并明确报告，不自行拍脑袋。
- 完成后给出：实际修改文件、执行过的验证、验证结果、仍存在的阻塞。
- 当前步骤未满足成功标准时，不进入下一步骤。
