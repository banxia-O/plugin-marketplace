# 插件百宝阁（科研版）｜开发计划

> 状态：V1 产品范围已冻结，按步骤开发
>
> 开发分支：`feature/wechat-miniapp-v1`
>
> 产品依据：`docs/10-科研Agent插件导航-PRD-v1.md`
>
> 执行规则：`AGENTS.md`

## 0. 总原则

本计划只实现微信小程序 V1，不重构现有网站，不迁移数据库，不新增 AI 推荐，不补做未来功能。

现有架构继续作为底盘：

```text
Cloudflare D1
    ↓
Cloudflare Worker API
    ├─ React Web（现有，保持正常）
    └─ 微信小程序（新增 packages/miniapp）
```

V1 核心闭环：

```text
打开小程序
  ↓
搜索 / 分类浏览
  ↓
插件卡片
  ↓
插件详情 + agent.md
  ↓
复制仓库地址 / agent.md
  ↓
交给桌面 Agent
```

### 全局成功标准

V1 完成时必须同时满足：

- 小程序可在微信开发者工具正常编译和预览；
- 真机可正常请求生产 Worker API；
- 搜索、分类、详情、复制、最近浏览完整可用；
- 首页存在 6 个科研高频分类快捷入口和“全部分类”；
- 分类与插件数据直接来自现有 API / D1，不维护第二份数据；
- V1 无登录、无普通用户上传、无 AI API、无排行榜；
- 隐私与科研敏感信息提醒可见；
- 小程序构建产物满足微信包体限制；
- 原 Web、Worker、审核服务现有构建与测试不因小程序改动而回归；
- 没有未解释的错误吞噬、无用抽象或为了未来功能提前写的代码。

## 1. 执行节奏

每次 Codex 任务只执行下面一个 Step，或 Step 中一个明确子步骤。

当前 Step 验收通过后，再进入下一 Step。需要半夏手动操作微信开发者工具、公众平台或真机时，代码完成后停下，给出明确操作路径和预期现象，不继续猜测结果。

---

# Step 0｜最小技术验证

## 目标

证明“现有 monorepo + Taro/React/TypeScript + 微信小程序 + Cloudflare Worker”这条链实际能跑通，再开始正式页面开发。

## 0.1 新建最小 miniapp 包

新增：

```text
packages/miniapp/
```

优先采用：

- Taro 4
- React
- TypeScript
- 微信小程序目标端

暂不引入：

- UI 组件库；
- 状态管理库；
- Markdown 渲染库；
- 请求封装库；
- 登录 SDK；
- AI SDK；
- 埋点 SDK。

只安装让一个空白小程序页面成功编译所需的最小依赖。

### 成功标准

- `packages/miniapp` 被现有 `pnpm-workspace.yaml` 自动识别；
- 小程序可单独执行开发构建；
- 产物目录可被微信开发者工具打开；
- 首页只需要显示“插件百宝阁（科研版）”测试文字；
- 不修改现有 Web / Worker 业务代码。

## 0.2 验证真实 API 请求

在最小页面中只做一次边界验证：

1. 请求现有 `/api/health`；
2. 请求 `/api/categories`；
3. 页面展示请求成功状态和分类数量即可。

API Base URL 必须集中配置，不能散落硬编码在页面组件中。

网络错误必须显式显示或抛出，不允许静默返回空数组伪装成功。

### 成功标准

- 微信开发者工具中请求成功；
- 配置 request 合法域名后，真机请求成功；
- `/api/categories` 返回真实线上分类；
- 如果真机因域名、备案、TLS 或微信平台配置失败，停止开发并报告实际错误，不通过代理层或 CloudBase 临时绕过去掩盖问题。

### 人工检查点

此处需要半夏完成微信公众平台 request 合法域名配置和真机验证。

**Step 0 未通过，不进入 Step 1。**

---

# Step 1｜最小数据访问层

## 目标

让小程序以最小代码读取 V1 真正需要的数据，同时复用现有数据契约。

## 需要的 API

仅实现小程序当前需要的三个读取能力：

```text
GET /api/categories
GET /api/plugins
GET /api/plugins/:slug
```

搜索继续通过 `/api/plugins?q=...` 完成，不额外造一套搜索 API。

## 共享类型策略

优先复用 `packages/shared` 的 TypeScript 类型，但要检查最终 bundle。

若直接引用 `@ppx/shared` 会把 Zod runtime 明显带入小程序包，则只做最小拆分，使 miniapp 可以引用纯类型；不要为了“架构漂亮”重构整个 shared 包。

### 成功标准

- 能获取分类列表；
- 能按 `q/category/subcategory/page/pageSize` 获取插件列表；
- 能按 slug 获取插件详情；
- API 错误向页面显式暴露；
- 没有登录、JWT、投稿相关代码进入 miniapp；
- 没有新增后端接口。

---

# Step 2｜首页

## 目标

完成 V1 首页的真实信息架构，不做视觉精修。

## 页面内容

按顺序实现：

1. 产品名：`插件百宝阁（科研版）`
2. 副标题：`给科研 Agent 找趁手的工具`
3. 科研敏感信息轻提示：
   `请勿输入患者身份信息、未公开研究数据或其他敏感信息。`
4. 搜索框
5. 最近浏览（无记录时隐藏）
6. 6 个科研高频分类快捷入口
7. “全部分类”入口
8. 关于 / 隐私入口

## 6 个快捷分类

只引用现有分类 slug：

```text
research   科研学术
search     信息检索
files      文件与文档
data       数据与数据库
ai         AI 与模型
dev        开发工具
```

这 6 个只是首页入口，不创建新表、不复制分类详情。

“全部分类”动态读取 `/api/categories`。

### 成功标准

- 6 个快捷入口可进入对应分类；
- 全部分类来自真实 API；
- 搜索框可带关键词进入搜索结果页；
- 首页不出现排行榜、半夏精选、用户上传、AI 推荐或登录入口；
- 网络失败时分类区域明确显示失败状态和重试操作。

---

# Step 3｜插件列表与搜索

## 目标

完成搜索结果页和分类页，共用同一种插件列表能力。

## 插件卡片最小字段

卡片只展示判断插件是否值得点进去所需的信息：

- 名称；
- 一句话简介；
- 分类标签（控制数量，避免卡片过高）；
- 部署方式；
- Stars；
- 审核状态 / agent.md 状态中确有价值的简要标记。

不要照搬 Web 卡片全部视觉元素。

## 搜索

- 空关键词不请求；
- 使用现有 `q` 参数；
- 中文和英文原样传给 API；
- 无结果显示明确空状态；
- 网络失败与“0 个结果”必须是两个不同状态。

## 分类

- 大类来自 `/api/categories`；
- 子分类继续沿用原数据结构；
- 点子分类后用现有查询参数筛选；
- 不建立小程序专属分类映射数据库。

## 分页

先实现满足移动端正常浏览的最小分页/加载更多方案。

不要预先实现虚拟列表、复杂缓存或无限滚动框架；只有真实列表性能证明需要时再加。

### 成功标准

- 搜索关键词能返回与网站同源的真实插件；
- 首页快捷分类和全部分类都能进入列表；
- 子分类筛选可用；
- 点击卡片进入正确详情 slug；
- API 错误可见；
- 不修改现有搜索后端。

---

# Step 4｜插件详情与 agent.md

## 目标

完成小程序最重要的“查到之后能交给 Agent”闭环。

## 详情字段

展示：

- 名称；
- 一句话简介；
- 分类；
- 部署方式；
- Stars；
- 最后仓库更新时间；
- 审核状态；
- agent.md 状态；
- 支持平台；
- 许可证；
- 原作者；
- `agent.md`。

核心操作：

- 复制仓库地址；
- 复制 `agent.md`。

## agent.md 渲染技术检查

Web 当前使用 `react-markdown`，不能默认直接搬进小程序。

先做最小 spike：确认在 Taro 微信端怎样以最小依赖可靠展示 Markdown。

优先级：

1. 能稳定显示标题、段落、列表、代码块、链接等当前 agent.md 常见内容；
2. 包体积小；
3. 不依赖浏览器 DOM；
4. 复制时使用原始 Markdown 文本，不复制渲染后的 HTML。

若需要引入 Markdown 库，先报告依赖体积和方案权衡，再加入。

### 成功标准

- 任意真实插件详情可正常打开；
- 有 agent.md 的插件可读；
- 无 agent.md 时显示真实状态，不生成替代内容；
- 两个复制操作真机可用；
- 页面不尝试在微信内“安装插件”。

---

# Step 5｜最近浏览

## 目标

完成无需登录的本地使用连续性。

使用微信本地 Storage，只存：

```ts
{
  slug,
  name,
  oneLiner,
  lastViewedAt
}
```

规则：

- 打开详情即写入；
- 同 slug 去重并置顶；
- 最多保存 20 条；
- 首页最多展示 6 条；
- 支持清空；
- 不上传服务器。

只处理 Storage API 本身真实可能抛出的边界错误；不要为内部对象结构添加层层防御。

### 成功标准

- 浏览多个插件后首页出现最近浏览；
- 重复浏览正确置顶；
- 超过 20 条正确淘汰最旧记录；
- 清空后区块消失；
- 重新打开小程序后记录仍存在；
- 网络断开时已保存的最近浏览入口仍可显示，但详情若需网络加载则明确提示网络错误。

---

# Step 6｜隐私、协议与关于页

## 目标

首版就把实际数据处理方式说清楚，不为了“看起来合规”调用不需要的隐私 API。

## 页面至少包含

- 用户协议；
- 隐私说明；
- 数据来源说明；
- 科研敏感信息提醒；
- 第三方 / 开源说明入口；
- 版本信息。

## V1 实际隐私边界

V1：

- 不要求微信登录；
- 不获取头像；
- 不获取昵称；
- 不获取手机号；
- 不上传最近浏览记录；
- 搜索词会作为 API 查询参数发送至现有服务器，因此提醒用户不要输入患者身份信息、未公开研究数据或其他敏感信息。

微信公众平台后台的《小程序用户隐私保护指引》由半夏按最终实际功能配置。

除非后续代码真实使用微信规定的隐私接口，否则不要为了防审核而额外调用 `wx.requirePrivacyAuthorize` 等接口。

### 成功标准

- 首页和关于页均可看到敏感信息提醒；
- 隐私说明与实际代码行为一致；
- 没有声明不存在的数据收集；
- 没有代码层面新增无必要个人信息采集。

---

# Step 7｜视觉收口与真机体验

## 目标

在所有功能已跑通后，再统一视觉，不在早期边写逻辑边反复抛光。

## 视觉来源

参考现有：

`docs/前端设计规范.md`

继承品牌特征：

- 淡紫主色；
- 清透、轻量；
- 白色卡片；
- 圆角；
- 信息层级清楚。

但不要照搬 Web：

- hover；
- 桌面双栏；
- Web Navbar；
- 浏览器专属交互。

优先保证手机单手操作和阅读密度。

### 真机检查

至少检查：

- 一台 Android；
- 一台 iPhone（若当时可获得）；
- 小屏宽度；
- 长 agent.md；
- 中文 / 英文混排；
- 长插件名；
- 网络较慢时 loading；
- 复制提示；
- 返回路径。

### 成功标准

- 60 秒主闭环可完成；
- 页面无明显溢出 / 遮挡 / 点击区域过小；
- 长文可读；
- 导航层级清楚；
- 不因视觉库导致包体明显膨胀。

---

# Step 8｜工程验收与回归

## 目标

确认新增小程序没有破坏原项目。

## 必做验证

根据仓库实际 scripts 执行可用项：

```text
pnpm install
pnpm typecheck
pnpm test
pnpm build
```

若根命令因为某 package 本来就没有对应 script 而行为不同，以 pnpm 实际规则和仓库现状为准，不添加“假测试脚本”只为让命令看起来绿色。

同时执行小程序独立构建，并记录：

- 构建是否成功；
- 主包大小；
- 总包大小；
- 是否误打入 Web / Worker 依赖。

### 成功标准

- 原有 Web typecheck/build/test 不出现由本分支引入的新失败；
- Worker 原测试不出现由本分支引入的新失败；
- miniapp typecheck/build 成功；
- 小程序包体满足微信当前正式限制；
- 没有 secrets / `.env` / AppSecret 进入 Git；
- `git diff main...feature/wechat-miniapp-v1` 中的修改均能解释为 V1 必需。

---

# Step 9｜微信上线前人工验收

此步骤主要由半夏在微信平台完成，Codex 只提供检查和必要修复。

检查项：

- 小程序名称审核；
- AppID 配置；
- 小程序 ICP / 认证；
- request 合法域名；
- 隐私保护指引；
- 服务类目；
- 用户协议；
- 真机预览；
- 体验版；
- 正式审核所需截图 / 说明；
- 审核被退回时按真实反馈最小修改。

不要在没有审核反馈时提前猜测并堆“防审核代码”。

### 最终成功标准

正式线上版本能够：

1. 打开；
2. 搜索；
3. 分类浏览；
4. 打开详情；
5. 阅读 `agent.md`；
6. 复制信息给桌面 Agent；
7. 保存本地最近浏览；
8. 查看隐私与数据来源说明。

---

# V1 结束后才允许讨论的内容

以下全部进入 V2 候选，不得在 V1 开发中顺手实现：

- AI 推荐；
- 自然语言科研任务理解；
- 微信登录；
- 云端收藏；
- 个性化推荐；
- 用户上传；
- 排行榜；
- 评论 / 社区；
- Agent 组合工作流；
- 微信 AI 生态增强；
- CloudBase 中转或迁移。

是否做 V2，由 V1 上线后的真实数据和比赛需要决定。
